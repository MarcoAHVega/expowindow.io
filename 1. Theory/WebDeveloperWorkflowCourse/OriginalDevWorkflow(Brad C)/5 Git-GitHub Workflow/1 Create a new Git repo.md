<!-- 
(6.3)
~ step 1
todo> `Create a new Git repo` from within the new project folder:
   - $ `git init`
   - $ `git add -A` or `git add "file.name"` to add this to the staging area
   - $ `git status`
   - $ `git commit -m 'initial commit'`
   - $ `git status`
   
   - $ `git checkout -- ` (to recover)
 -->