<!-- 
(29.12)
& 5 push a master branch to github
~ step 1
todo> Now let's go ahead and `push our master branch up to github` so we can see a visualization of all the comments. So let's run 
   ^
   git push origin master

   ? Once we merged `git took the commit history of the count-to-ten branch and added it to the master branch history`. 
-----------------------------------------------------------------------------
 -->