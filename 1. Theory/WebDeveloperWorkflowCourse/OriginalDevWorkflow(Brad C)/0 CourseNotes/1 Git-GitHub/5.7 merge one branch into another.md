<!-- 
(29.11)
& 4 how to merge one branch into another
todo> So now that our count to ten feature `is complete it's no longer a work in progress`, We can `merge it into our master branch`. 

~ step 1
todo> So to do that let's `switch back to our master branch`. 
   ^
   git checkout master

~ step 2
todo> So in the command line `while we have the master branch checked out`. Let's run 
   ^
   git merge count-to-ten 
   
   * So we still have the master branch checked out. But if we check the browser there's the list completed

---------------------------------------------------------------------------
 -->