<!-- 
(29.16)(git branch -d count-to-15)
& 8 Let's `keep the local copy of our repo clean` by deleting unnecessary branches.

   todo> So if we run the `git branch` command to list all of the branches
   ^
   git branch
    
   ? we see `count to 15` which was deleted on the Github servers but it wasn't deleted in our local copy of the repo. 

   todo> So let's go ahead and `delete that locally`.
   ^
   git branch -d count-to-15

   todo> Let's also `delete the count-to-10` branch
   ^
   git branch -d count-to-10
-------------------------------------------------------------------------
 -->